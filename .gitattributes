# Normalize everything to LF; the repo is edited from both Windows and WSL.
* text=auto eol=lf

# Windows-only files keep CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets
*.png binary
*.ico binary
*.db binary
